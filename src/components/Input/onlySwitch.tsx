import React from "react";
import { Switch } from "@mui/material";

const UuionlySwitch = ({ ...props }) => {
  return (
    <Switch color="primary" value={undefined} />
  );
};

export default UuionlySwitch;
